package library;

import org.mindrot.jbcrypt.BCrypt;

public class Main {
    public static void main(String[] args) {
       /*
           // In your Main.java (or UI layer) for making a member account
            try {
                Member member = new Member("STU_123", "Alice", "alice@gmail.com", ...);
            } catch (IllegalArgumentException e) {
                System.err.println("Error: " + e.getMessage()); // Your message appears here!
                // Retry input or exit
            }
        */

        // Sign up
//        boolean passwordValid = BCrypt.checkpw();
    }
}
